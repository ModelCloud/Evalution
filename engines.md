# Engines

The canonical engine guide lives in [docs/engine.md](docs/engine.md).

Use that document for backend contracts, built-in engine behavior, and implementation guidance for
new runtimes such as `LlamaCpp`, `VLLM`, `SGLang`, `TensorRTLLM`, `OpenVINO`, and the transformer
engines.
